# @package _global_

data:
  train:
    n_files: null
    jet_physics_cfg:
      max_cnstits: 202
      max_ctxt_cnstits: 302
  loader_config:
    batch_size: 512
    num_workers: 12

model:
  num_layers: 4
  # defaults:
  # - override architectures@encoder_cfg: perceiver_cfg.yaml
  # - override architectures@decoder_cfg: perceiver_cfg.yaml
